# --- Shared Configuration ---
ENVIRONMENT=development

# --- Backend (Go) Configuration ---
BACKEND_PORT=8080
INTELLIGENCE_SERVICE_ADDR=localhost:50051
MCP_SERVER_ADDR=localhost:50052

# --- Intelligence (Python) Configuration ---
INTELLIGENCE_PORT=50051

# GCP (Current Active Provider)
GCP_PROJECT=your-project-id
GCP_REGION=us-central1
TEXT_MODEL_ID=gemini-1.5-flash
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json

# --- RAG Configuration ---
# Get your Search Engine ID from https://programmablesearchengine.google.com/
# Ensure Custom Search and Fact Check APIs are enabled in your GCP project.
GOOGLE_SEARCH_CX=your-search-engine-id

# MCP (Future Active Provider)
MCP_ACTIVE=false
MCP_SERVER_ENDPOINT=http://localhost:50052/v1
MCP_API_KEY=your-mcp-key
